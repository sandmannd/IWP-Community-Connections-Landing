IWP Community Connections — M7.3 D1 Shadow Reads

PURPOSE
M7.3 compares the public landing-page payload generated by the live Google Apps Script backend with a payload reconstructed from Cloudflare D1.

PRODUCTION SAFETY
- Google Apps Script remains the live source of truth.
- No public page reads from D1 yet.
- No organizer page reads from D1 yet.
- No writes are sent to D1.
- productionCutover remains false.

NEW ENDPOINT
GET /api/migration-shadow-read

The endpoint:
1. Calls the live Apps Script landingDataApi endpoint.
2. Builds the equivalent public landing payload from D1.
3. Compares stats, featured adventure, and the first four upcoming adventures.
4. Reports exact mismatches without changing live traffic.

EXPECTED RESULT AFTER DEPLOYMENT
{
  "success": true,
  "migration": "M7.3",
  "productionCutover": false,
  "googleLive": true,
  "d1ShadowRead": true,
  "match": true,
  "mismatchCount": 0
}

If match is false, do not cut public reads over to D1. The mismatch report is the diagnostic for the next correction/sync step.

DEPLOYMENT
Landing repository only.
Commit: M7.3 D1 shadow reads
Push origin and allow Cloudflare Pages to deploy.
No Apps Script repository change and no clasp push.
