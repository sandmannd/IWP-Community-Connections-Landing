IWP COMMUNITY CONNECTIONS - SPRINT 8.12G

What changed
- Replaced the Apps Script header with the actual landing-page header structure.
- Uses the exact landing-page top header artwork and main banner artwork.
- Uses the landing-page navigation spacing, typography, active underline, and mobile menu behavior.
- Kept the Apps Script navigation actions wired to the application.
- Added a working Featured Adventure navigation action.
- Removed the previous layered and duplicated header fixes from visual control by adding one final authoritative style block.

Install
1. Copy every file in this package into the existing IWP-Community-Connections repository folder.
2. Replace matching files.
3. Run clasp push.
4. Update the Apps Script deployment if required.

Git
Commit: Sprint 8.12G - Match app header to landing page

This package contains no .git directory.
