IWP Community Connections — M6.16 Event-Day Check-In

Landing repository only.

Changes:
- Check-In Mode now gives the search box event-day-specific instructions.
- When a search narrows to one party, Enter still checks them in; after a successful check-in the search clears and is ready for the next person.
- Checked-in parties now have a prominent "Checked In" marker and distinct card styling when shown.
- The Undo Check-In action is visually secondary so it cannot be confused with the primary Check In Party action.
- Added explicit offline, back-online, timeout, and registration-service status messaging.
- Existing loaded registrations stay visible when a refresh fails instead of replacing the page with a misleading sign-in error.
- Auto-refresh pauses while the browser is offline and refreshes when connectivity returns.
- No Apps Script/backend changes.
- .git excluded.
