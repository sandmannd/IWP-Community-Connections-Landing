(function(){'use strict';
var cfg=window.IWP_SITE_CONFIG||{},appUrl=String(cfg.appUrl||''),sessionKey='iwpOrganizerSessionV1',eventId=new URLSearchParams(location.search).get('id')||'',request=0,timer=0,data=null;
function byId(id){return document.getElementById(id)}function text(id,v){var e=byId(id);if(e)e.textContent=v==null?'':String(v)}function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]})}
function areas(){var a=[];try{a.push(sessionStorage)}catch(e){}try{a.push(localStorage)}catch(e){}return a}function readSession(){for(var i=0;i<areas().length;i++){try{var s=JSON.parse(areas()[i].getItem(sessionKey)||'null');if(s&&s.token&&s.expiresAt&&Date.parse(s.expiresAt)>Date.now())return s}catch(e){}}return null}
function state(name){byId('registrationLoading').hidden=name!=='loading';byId('registrationAccess').hidden=name!=='access';byId('registrationWorkspace').hidden=name!=='workspace'}function fail(msg){clearTimeout(timer);state('access');text('registrationAccessMessage',msg||'Return to the Organizer Command Center and sign in.')}
function jsonp(){var session=readSession();if(!session){fail();return}var s=document.createElement('script');s.id='registrationApi'+(++request);s.src=appUrl+(appUrl.indexOf('?')<0?'?':'&')+'api=organizer-registrations&callback=iwpOrganizerRegistrationsCallback&session='+encodeURIComponent(session.token)+'&eventId='+encodeURIComponent(eventId)+'&_='+Date.now();s.onerror=function(){fail('Unable to reach the organizer registration service.')};document.head.appendChild(s);timer=setTimeout(function(){fail('The organizer registration service took too long to respond.')},45000)}
function eventTitle(){var e=data&&data.event||{};return e.title||e.Title||'Adventure'}function eventDetail(){var e=data&&data.event||{},parts=[];if(e.startDate||e.StartDate)parts.push(e.startDate||e.StartDate);if(e.startTime||e.StartTime)parts.push(e.startTime||e.StartTime);if(e.location||e.LocationName)parts.push(e.location||e.LocationName);return parts.join(' · ')}
function statusClass(status){return String(status||'').toLowerCase().replace(/[^a-z0-9]+/g,'-')}function render(){var q=byId('registrationSearch').value.trim().toLowerCase(),status=byId('registrationStatus').value;var rows=(data.registrations||[]).filter(function(r){var rs=String(r.status||'').toLowerCase();var hay=[r.name,r.email,r.phone,r.status,r.paymentStatus,r.adultGuestNames,r.childNames].join(' ').toLowerCase();return (status==='all'||rs===status)&&(!q||hay.indexOf(q)>=0)});text('registrationResultsSummary','Showing '+rows.length+' of '+(data.registrations||[]).length+' registrations');var el=byId('registrationList');if(!rows.length){el.innerHTML='<div class="organizer-empty">No registrations match your filters.</div>';return}el.innerHTML=rows.map(function(r){var people=Number(r.adults||0)+Number(r.children||0),tags=['<span class="organizer-chip '+statusClass(r.status)+'">'+esc(r.status||'Registered')+'</span>','<span class="organizer-chip">'+people+' '+(people===1?'person':'people')+'</span>'];if(r.paymentStatus)tags.push('<span class="organizer-chip">'+esc(r.paymentStatus)+'</span>');if(r.acknowledgementsComplete)tags.push('<span class="organizer-chip">Acknowledged</span>');return '<article class="organizer-registration-card"><div><h3>'+esc(r.name||'Unnamed registration')+'</h3><p>'+esc(r.adults||0)+' adult'+(Number(r.adults||0)===1?'':'s')+(Number(r.children||0)?' · '+esc(r.children)+' child'+(Number(r.children)===1?'':'ren'):'')+'</p>'+(r.adultGuestNames?'<p>Adult guests: '+esc(r.adultGuestNames)+'</p>':'')+(r.childNames?'<p>Children: '+esc(r.childNames)+'</p>':'')+(r.notes?'<p>Notes: '+esc(r.notes)+'</p>':'')+'<div class="organizer-registration-tags">'+tags.join('')+'</div><small>Registered '+esc(r.registeredAt||'')+'</small></div><div class="organizer-registration-contact">'+(r.email?'<a href="mailto:'+encodeURIComponent(r.email)+'">'+esc(r.email)+'</a>':'')+(r.phone?'<a href="tel:'+encodeURIComponent(r.phone)+'">'+esc(r.phone)+'</a>':'')+'</div></article>'}).join('')}
window.iwpOrganizerRegistrationsCallback=function(p){clearTimeout(timer);if(!p||!p.success){var m=p&&p.error||'Unable to load registrations.';if(/session|sign in|expired|authorized/i.test(m)){fail(m);return}state('workspace');text('registrationPageHeading','Unable to load registrations');return}data=p.data||{};state('workspace');text('registrationPageHeading',eventTitle()+' Registrations');text('registrationEventTitle',eventTitle());text('registrationEventDetails',eventDetail());var c=data.counts||{};text('registrationCount',c.registrations||0);text('peopleCount',c.people||0);text('waitlistCount',c.waitlist||0);text('checkedInCount',c.checkedIn||0);byId('emailParticipantsLink').href='/organizer-email.html?id='+encodeURIComponent(eventId)+'&task='+encodeURIComponent('Event Update');byId('editAdventureLink').href='/organizer-builder.html?id='+encodeURIComponent(eventId);render()};
async function lifecycle(action){var session=readSession();if(!session){fail();return}var title=eventTitle();if(action==='cancel'){if(!confirm('Cancel '+title+'? Registrations and history will be preserved, but registration will close.'))return}else{var typed=prompt('Permanently delete '+title+' and all associated registrations, tasks, resources, queued emails, and email history?\n\nType DELETE to continue.');if(typed!=='DELETE')return}var button=byId(action==='cancel'?'cancelAdventureButton':'deleteAdventureButton');button.disabled=true;var old=button.textContent;button.textContent=action==='cancel'?'Cancelling…':'Deleting…';try{var r=await fetch('/api/organizer-event-action',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({appUrl:appUrl,session:session.token,eventId:eventId,eventAction:action})});var result=await r.json();if(!r.ok||!result.success)throw new Error(result.error||'Adventure action failed.');alert(result.message||'Adventure updated.');location.href='/organizer-adventures.html'}catch(e){alert(e.message||'Adventure action failed.');button.disabled=false;button.textContent=old}}
byId('registrationSearch').addEventListener('input',render);byId('registrationStatus').addEventListener('change',render);byId('cancelAdventureButton').addEventListener('click',function(){lifecycle('cancel')});byId('deleteAdventureButton').addEventListener('click',function(){lifecycle('delete')});if(!eventId||!appUrl)fail('The event or organizer service is missing.');else jsonp();
})();
