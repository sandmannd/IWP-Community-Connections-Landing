IWP Community Connections M6.1

Performance update:
- Landing page renders recently cached adventure data immediately, then refreshes from Apps Script.
- Organizer Command Center renders recently cached dashboard data immediately, then refreshes in the background.
- Temporary slow responses no longer replace useful cached content with an error or sign-in screen.
- Cache is short-lived and scoped to the organizer session.
