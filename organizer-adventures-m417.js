(function(){
  'use strict';
  var config=window.IWP_SITE_CONFIG||{},appUrl=String(config.appUrl||''),clientId=String(config.googleClientId||''),credentialKey='iwpOrganizerGoogleCredential',adventures=[],requestNumber=0,activeTimeout=0;
  function byId(id){return document.getElementById(id);}function text(id,value){var el=byId(id);if(el)el.textContent=value==null?'':String(value);}function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c];});}
  function clearTimer(){if(activeTimeout){clearTimeout(activeTimeout);activeTimeout=0;}}function removeRequests(){Array.prototype.slice.call(document.querySelectorAll('script[id^="organizerAdventureApi"]')).forEach(function(node){if(node.parentNode)node.parentNode.removeChild(node);});}
  function setState(name){var loading=byId('organizerLoading'),access=byId('organizerAccess'),workspace=byId('organizerAdventureWorkspace');if(loading)loading.hidden=name!=='loading';if(access)access.hidden=name!=='access';if(workspace)workspace.hidden=name!=='workspace';var out=byId('organizerSignOutTop');if(out)out.hidden=name!=='workspace';}
  function savedCredential(){try{return sessionStorage.getItem(credentialKey)||'';}catch(ignore){return '';}}function saveCredential(v){try{if(v)sessionStorage.setItem(credentialKey,v);else sessionStorage.removeItem(credentialKey);}catch(ignore){}}
  function showAccess(message){clearTimer();removeRequests();setState('access');text('organizerAccessMessage',message||'Sign in with the approved Google account used for Community Connections.');renderGoogleButton();}
  function signOut(){clearTimer();removeRequests();saveCredential('');if(window.google&&google.accounts&&google.accounts.id)google.accounts.id.disableAutoSelect();location.href='/organizer.html';}
  var menu=document.querySelector('.mobile-menu-button'),nav=byId('mainNavigation');if(menu&&nav){menu.addEventListener('click',function(){var open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-open',!open);menu.textContent=open?'Menu':'Close';});}var out=byId('organizerSignOutTop');if(out)out.addEventListener('click',signOut);
  function dateDisplay(item){var start=String(item.startDate||''),end=String(item.endDate||'');if(!start)return 'Date not set';if(!end||end===start)return start+(item.startTime?' · '+item.startTime:'');return start+' through '+end;}function statusClass(status){var s=String(status||'').toLowerCase();return s==='published'?'is-published':s==='draft'?'is-draft':s==='cancelled'?'is-cancelled':'';}
  function render(){var search=byId('adventureSearch'),select=byId('adventureStatus'),q=String(search&&search.value||'').trim().toLowerCase(),status=String(select&&select.value||'all').toLowerCase();var filtered=adventures.filter(function(a){var matchStatus=status==='all'||String(a.status||'').toLowerCase()===status;var hay=[a.title,a.location,a.type,a.status].join(' ').toLowerCase();return matchStatus&&(!q||hay.indexOf(q)!==-1);});text('adventureResultsSummary','Showing '+filtered.length+' of '+adventures.length+' adventures');var el=byId('organizerAdventureList');if(!el)return;if(!filtered.length){el.innerHTML='<div class="organizer-empty">No adventures match your filters.</div>';return;}el.innerHTML=filtered.map(function(a){var cap=a.maxParticipants?esc(a.peopleCount)+' of '+esc(a.maxParticipants)+' people':esc(a.peopleCount)+' people · Unlimited capacity';var wait=Number(a.waitlistCount||0)>0?'<span class="organizer-chip">'+esc(a.waitlistCount)+' waitlisted</span>':'';var closed=a.registrationClosed?'<span class="organizer-chip">Registration closed</span>':'';return '<article class="organizer-adventure-card"><div><span class="organizer-eyebrow">'+esc(a.type||'Adventure')+'</span><h2>'+esc(a.title)+'</h2><p>'+esc(dateDisplay(a))+(a.location?' · '+esc(a.location):'')+'</p><div class="organizer-adventure-meta"><span class="organizer-chip '+statusClass(a.status)+'">'+esc(a.status||'Draft')+'</span><span class="organizer-chip">'+cap+'</span><span class="organizer-chip">'+esc(a.registrationCount||0)+' registrations</span>'+wait+closed+'</div></div><div class="organizer-card-actions"><a class="primary" href="/adventure.html?id='+encodeURIComponent(a.eventId||'')+'">View Adventure</a><a class="secondary" href="/organizer.html">Command Center</a></div></article>';}).join('');}
  window.iwpOrganizerAdventuresCallback=function(payload){clearTimer();removeRequests();if(!payload||payload.success!==true||payload.authorized!==true){if(payload&&payload.authenticated&&payload.user&&payload.user.email)saveCredential('');showAccess(payload&&payload.error||'Organizer authorization was not accepted.');return;}adventures=Array.isArray(payload.adventures)?payload.adventures:[];text('adventureTotalBadge',adventures.length);setState('workspace');try{render();}catch(error){console.error('Organizer adventure list render failed:',error);text('adventureResultsSummary','Adventures loaded; the list could not be displayed.');}};
  function load(credential){clearTimer();removeRequests();if(!appUrl){showAccess('The organizer service URL has not been configured.');return;}if(!credential){showAccess();return;}setState('loading');var script=document.createElement('script');script.async=true;script.id='organizerAdventureApi'+(++requestNumber);script.src=appUrl+(appUrl.indexOf('?')===-1?'?':'&')+'api=organizer-adventures&callback=iwpOrganizerAdventuresCallback&credential='+encodeURIComponent(credential)+'&_='+Date.now();script.onerror=function(){showAccess('Unable to reach the organizer service. Please sign in again.');};document.head.appendChild(script);activeTimeout=setTimeout(function(){var loading=byId('organizerLoading');if(loading&&!loading.hidden)showAccess('The organizer service took too long to respond. Please sign in again.');},20000);}
  function handleGoogleCredential(response){var credential=response&&response.credential?String(response.credential):'';if(!credential){showAccess('Google did not return a sign-in credential. Please try again.');return;}saveCredential(credential);load(credential);}function renderGoogleButton(){var target=byId('googleSignInButton');if(!target||target.dataset.rendered==='true')return;if(!clientId){text('organizerAccessMessage','The Google sign-in client has not been configured.');return;}if(!(window.google&&google.accounts&&google.accounts.id)){setTimeout(renderGoogleButton,150);return;}google.accounts.id.initialize({client_id:clientId,callback:handleGoogleCredential,auto_select:false,cancel_on_tap_outside:true});google.accounts.id.renderButton(target,{theme:'outline',size:'large',shape:'pill',text:'signin_with',width:300});target.dataset.rendered='true';}
  var search=byId('adventureSearch'),select=byId('adventureStatus');if(search)search.addEventListener('input',render);if(select)select.addEventListener('change',render);var existing=savedCredential();if(existing)load(existing);else showAccess();
})();
