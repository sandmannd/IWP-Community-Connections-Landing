M5.5.1 adds the Cancel Adventure and Delete Adventure controls to the bottom of the Cloudflare Adventure Builder edit page.

Cancel preserves registrations and history while closing registration and marking the adventure cancelled.
Delete requires typing DELETE and calls the existing M5.5 organizer lifecycle endpoint.
The danger zone is hidden when creating a new unsaved adventure.
